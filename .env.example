# Copy this file to .env and fill in values as needed.
# If MONGO_URI is left blank, the app runs in JSON fallback (demo) mode —
# safe for hackathon demos without any DB setup.

PORT=4000
MONGO_URI=

